{
  "private": true,
  "workspaces": [
    "client",
    "server",
    "shared"
  ],
  "scripts": {
    "client": "yarn workspace dng-client start",
    "server": "yarn workspace dng-server start",
    "lint": "eslint --ext .ts client server"
  }
}